# Orblo frontend

See the [root README](../README.md) for what this project does and how to run it (frontend + backend).

Quick start from this directory:

```bash
npm install
cp .env.example .env   # add a WalletConnect project ID
npm run dev
```
